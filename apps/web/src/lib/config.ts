// Server-side limits only. Do not add secrets or sensitive values here.
export const PAGE_SIZE = 10;
export const MAX_HTML_BYTES = 5 * 1024 * 1024; // 5 MB
export const MAX_FILES = 20;
export const MAX_FILE_SIZE = 10 * 1024 * 1024; // 10 MB per file
